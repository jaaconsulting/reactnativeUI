import React from 'react';
import { Text, View } from 'react-native';
import { utils } from '../../libs/themes/theme';

const FormFieldsScreen = () => (
    <View style={utils.rootScreen}>
        <Text>FormFieldsScreen</Text>
    </View>
);

export default FormFieldsScreen;
